import 'mocha';
import {expect} from 'chai';
import {FilterMapAddReduce} from '../../src/ejercicio9/filterMapAddReduce';
import {FilterMapSubReduce} from '../../src/ejercicio9/filterMapSubReduce';

describe( 'Test de FilterMapAddReduce', () => {
  it('Existe el método factoryMethod()', () => {
    // expect().to.exist;
    // expect().to.be.a('function');
    // expect().to.be.instanceOf();
    // expect().to.be.equal('');
    // expect().to.be.equal('');
  });
});
